✅ 8. Exception Types
# Class-based exception
class MyError(Exception):
    pass

try:
    raise MyError("Custom Error")
except MyError as e:
    print(e)

# Nested exception
try:
    try:
        x = int("abc")
    except ValueError:
        print("Inner Exception")
except:
    print("Outer Exception")
✅ 9. Lambda Function
square = lambda x: x*x
print(square(5))
✅ 10. filter() and reduce()
from functools import reduce

# filter
nums = [1, 2, 3, 4, 5]
even = list(filter(lambda x: x%2==0, nums))
print("Even:", even)

# reduce
sum_all = reduce(lambda a, b: a+b, nums)
print("Sum:", sum_all)
✅ 11. Module Creation & Usage

👉 Create file: mymodule.py

def greet(name):
    return "Hello " + name

👉 Main file:

import mymodule
print(mymodule.greet("Uday"))
✅ 12. Image Insertion
from PIL import Image

img = Image.open("image.jpg")
img.show()
✅ 13. DataFrame & CSV
import pandas as pd

# create dataframe
data = {"Name": ["Uday", "Ram"], "Age": [21, 22]}
df = pd.DataFrame(data)

# save csv
df.to_csv("data.csv", index=False)

# read csv
df2 = pd.read_csv("data.csv")
print(df2)
✅ 14. GUI using Tkinter
import tkinter as tk

def click():
    label.config(text="Button Clicked")

root = tk.Tk()
root.title("My App")

label = tk.Label(root, text="Hello")
label.pack()

button = tk.Button(root, text="Click Me", command=click)
button.pack()

root.mainloop()